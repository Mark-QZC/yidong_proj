# 接口

### DVFS

void dvfs_MHz_mode_change ( 模式号 )
返回值 成功 1， 失败 0

### 软硬转发

c语言：int hardtrans_open(){

​	硬转发开

}

void hardtrans_close(){

​	硬转发关

}

### DPM

建议抽象成几个通用内核 API，让他们不关心硬件细节：

1. 电源状态查询
   enum pwr_state { PWR_ON, PWR_IDLE, PWR_SLEEP, PWR_OFF };
   enum pwr_state dpm_get_state(device_t *dev);
2. 进入低功耗/唤醒
   int dpm_enter_low_power(device_t *dev);
   int dpm_wakeup(device_t *dev);
3. 状态变更通知（回调）
   int dpm_register_notifier(struct notifier_block *nb);
   int dpm_unregister_notifier(struct notifier_block *nb);

